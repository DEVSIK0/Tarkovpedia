version: "3.9"

services:
  web:
    container_name: tarkovpedia_web
    build:
      context: ..
      dockerfile: ./docker/Dockerfile.web
    ports:
      - "3000:3000"
    environment:
      HOST: "0.0.0.0"
      PORT: "3000"
      DATABASE_URL: "file:./data/tarkovpedia.db"
    volumes:
      - tarkovpedia_data:/app/data
    restart: unless-stopped

volumes:
  tarkovpedia_data:
    driver: local
